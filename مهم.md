

خطة التحسين بالترتيب

أخذ نسخة احتياطية أولًا

قبل تعديل الملفات أو قاعدة البيانات:

نزّل نسخة من ملفات النظام.
خذ نسخة من قاعدة البيانات.
لا تحذف ملفات السجلات أو الجلسات قبل التأكد من وجود نسخة احتياطية.

يمكن إدارة النسخ من قسم Backups في hPanel.

معالجة سبب وصول التخزين إلى الحد

الفحص أظهر أن حد عمليات التخزين IOPS تم الوصول إليه. أكثر الأسباب شيوعًا:

النظام يكتب ملفًا مع كل طلب.
إنشاء ملف جلسة جديد لكل مستخدم بشكل متكرر.
تسجيل كل عملية دخول أو فتح صفحة داخل ملف Log.
وجود مهمة مجدولة تعمل كل دقيقة أو عدة مرات في الساعة.
رفع أو قراءة ملفات كثيرة من القرص بدل استخدام قاعدة البيانات أو التخزين المؤقت.
عمليات نسخ احتياطي أو تنظيف تعمل بشكل متكرر.

راجع داخل النظام مجلدات مثل:

`logs`
`cache`
`storage`
`sessions`
`tmp`
`uploads`

لا تحذفها مباشرة؛ افحص حجمها وعدد الملفات فيها أولًا، ثم اجعل النظام ينظف الملفات القديمة تلقائيًا.

تقليل مهام Cron

راجع Cron Jobs في hPanel، وابحث عن المهام التي تعمل كل دقيقة أو كل خمس دقائق.

يفضل:

تشغيل المهام غير الضرورية كل 15 أو 30 دقيقة.
دمج عدة مهام في مهمة واحدة.
إيقاف المهام القديمة أو المكررة.
عدم تشغيل نسخ احتياطي أو تقارير ثقيلة أثناء أوقات الاستخدام.
جعل المهمة تعالج عددًا محدودًا من السجلات في كل تشغيل.

مهمة تقوم بمعالجة آلاف السجلات في كل مرة قد تستهلك التخزين والمعالج حتى لو كان عدد المستخدمين قليلًا.

تحسين قاعدة البيانات

داخل قاعدة البيانات:

أضف فهارس `Indexes` للأعمدة المستخدمة في:
تسجيل الدخول.
البحث.
التصفية.
العلاقات بين الجداول.
الترتيب حسب التاريخ.
تجنب استخدام `SELECT *` إذا كانت الصفحة تحتاج بضعة أعمدة فقط.
أضف حدودًا للنتائج مثل `LIMIT`.
لا تنفذ استعلامًا جديدًا لكل عنصر داخل قائمة؛ اجمع البيانات باستعلام واحد.
احذف السجلات المؤقتة والقديمة، مثل سجلات الدخول والتنبيهات المنتهية.
لا تجعل صفحة لوحة التحكم تعرض آلاف السجلات دفعة واحدة.

مثال عام لاستعلام أفضل:

```sql
SELECT id, username, status
FROM users
WHERE status = &apos;active&apos;
ORDER BY id DESC
LIMIT 50;
```

بدل تحميل جدول المستخدمين كاملًا في كل مرة.

تفعيل التخزين المؤقت

استخدم التخزين المؤقت للصفحات والبيانات التي لا تتغير مع كل طلب، مثل:

القوائم الثابتة.
إعدادات النظام.
الإحصاءات التي لا تحتاج تحديثًا لحظيًا.
الملفات CSS وJavaScript والصور.

بالنسبة للنظام المخصص، يجب أن يكون التخزين المؤقت مدمجًا في الكود أو يتم تفعيله من إعدادات الاستضافة إذا كان متاحًا. أما صفحات تسجيل الدخول ولوحة التحكم والصفحات التي تعرض بيانات خاصة بالمستخدم، فلا ينبغي تخزينها مؤقتًا بشكل عام.

تحسين PHP والكود

بما أن النظام ليس WordPress، راجع النقاط التالية في الكود:

عدم فتح وقراءة ملف في كل طلب دون حاجة.
عدم تسجيل رسائل Debug في الموقع المباشر.
إغلاق اتصالات قاعدة البيانات والملفات بعد استخدامها.
عدم تنفيذ استعلامات داخل حلقات متكررة.
استخدام `prepared statements`.
منع تحميل مكتبات كبيرة في كل صفحة إذا لم تكن مطلوبة.
تشغيل OPcache إن كان متاحًا لإبقاء ملفات PHP المترجمة في الذاكرة.

ضغط الملفات والصور

لتقليل استهلاك الموارد:

حوّل الصور إلى WebP أو AVIF.
صغّر ملفات CSS وJavaScript.
استخدم التحميل الكسول للصور.
لا ترفع صورًا ضخمة ثم تعرضها بحجم صغير.
فعّل CDN للملفات الثابتة إذا كان مناسبًا.

هذا يقلل عدد طلبات الخادم، لكنه لن يحل وحده مشكلة IOPS إذا كان النظام يكتب ملفات كثيرة أو ينفذ استعلامات ثقيلة.

مراجعة تسجيل الدخول

بما أن النظام يستخدم مسار `/login`، افحص أن تسجيل الدخول لا يقوم عند كل محاولة بـ:

إنشاء سجلات متعددة بلا تنظيف.
حفظ بيانات كبيرة في الجلسة.
كتابة ملف جديد لكل محاولة.
تنفيذ عدة استعلامات متكررة.
تحميل جميع المستخدمين بدل البحث عن البريد أو اسم المستخدم مباشرة.

يجب أن يكون البحث عن المستخدم مفهرسًا، مثلًا على البريد الإلكتروني أو اسم المستخدم.

اختبار النظام بعد كل تعديل

بعد كل تغيير، راقب:

سرعة تسجيل الدخول.
سرعة لوحة التحكم.
زمن تحميل الصفحات.
استهلاك التخزين.
عدد عمليات Cron.
أخطاء PHP وقاعدة البيانات.
هل عاد حد IOPS للارتفاع أم لا.

لا تغيّر عدة أشياء كبيرة في الوقت نفسه؛ حتى تعرف أي تعديل حل المشكلة.

هل تنتقل إلى VPS الآن؟

لا أنصح بالانتقال فورًا. عدد المستخدمين المتوقع، وهو 30 إلى 40 مستخدمًا يوميًا، منخفض نسبيًا، والأنظمة الفرعية الثلاثة ذات الضغط المنخفض يمكن أن تعمل على نفس الاستضافة إذا كان الكود وقاعدة البيانات محسّنين.

انتقل إلى VPS فقط إذا تحقق أحد هذه الشروط:

النظام يحتاج Node.js أو Python أو Docker.
يوجد مستخدمون كثيرون في الوقت نفسه.
توجد عمليات معالجة مستمرة أو تقارير ثقيلة.
وصلت حدود الاستضافة مرة أخرى بعد التحسين.
تحتاج إعدادات خادم خاصة لا يوفرها الشيرد.

القرار العملي: ابدأ بتحسين السجلات وCron وقاعدة البيانات والتخزين المؤقت، ثم أعد اختبار الأداء. إذا استمر الوصول إلى حد IOPS رغم التحسين، عندها يصبح VPS خيارًا منطقيًا.